name: conditional-residual

entry_points:
    train-baseline-segmentation:
        command: "python -m conditional_residual.train_baseline_segmentation"

    train-baseline-detection:
        command: "python -m conditional_residual.train_baseline_detection"

    train-scalable-segmentation:
        command: "python -m conditional_residual.train_scalable_segmentation"

    train-structured-detection:
        command: "python -m conditional_residual.train_structured_detection"

    test-cityscapes:
        command: "python -m conditional_residual.test_cityscapes"

    test-coco:
        command: "python -m conditional_residual.test_coco"
